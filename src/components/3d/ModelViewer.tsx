/**
 * 3D Model Viewer Component
 * Uses React Three Fiber to display GLTF/GLB models
 * Must be dynamically imported to avoid SSR issues
 */

'use client';

export function ModelViewer() {
    return null; // To be implemented with R3F Canvas
}
